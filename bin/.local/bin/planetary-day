#!/usr/bin/env bash
# prints the planetary day (silly)
set -euo pipefail

planet() {
  declare -A symbols=(
    [Sun]="☉" [Mon]="☽" [Tue]="♂" [Wed]="☿"
    [Thu]="♃" [Fri]="♀" [Sat]="♄"
  )

  local day
  day=$(date +%a)
  echo "${symbols[$day]}"
}

planet
